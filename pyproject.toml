[project]
name = "shopops-codex-marketplace"
version = "0.1.5"
requires-python = ">=3.12"

[project.optional-dependencies]
test = [
  "PyYAML>=6.0",
  "pytest>=9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests", "plugins/shopops-onboarding/tests"]
